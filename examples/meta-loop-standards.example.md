# 点検基準（プロジェクト例）

このファイルをプロジェクトの `.pi/meta-loop-standards.md` として置くと、
拡張デフォルトの基準に**追加**で読み込まれる。

## このプロジェクトのルール
- UI テキストは全て strings リソースに置く（ハードコード禁止）
- DB マイグレーションは必ず後方互換（ロールバック可能）にする

## このプロジェクトの禁止事項
- `legacy/` ディレクトリへの変更禁止
- サードパーティ依存の追加は事前にユーザー確認が必要
